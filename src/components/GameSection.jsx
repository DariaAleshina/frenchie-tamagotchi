function GameSection({ children }) {
  return (
    <section className="lg:w-3xl mr-auto ml-auto flex justify-center flex-col items-center gap-5">
      {children}
    </section>
  );
}

export default GameSection;
